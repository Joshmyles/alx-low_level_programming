#include "main.h"

/**
 * mul - is a function that multipies 2 integers
 * @a: first parameter
 * @b: second parameter
 * Return: the multiple of the two
 */

int mul(int a, int b)
{
return (a * b);
}
